# Chores — 1024×600

**Job:** Open chores list; tap → confirm → complete.  
**Contract:** `chores.items[]` (`id`, `title`, optional `assignee_name`).  
**Mutations:** `POST /api/chores/{id}/complete` only.
