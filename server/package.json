{
  "name": "family-hub-server",
  "version": "0.1.0",
  "private": true,
  "description": "Family Hub v0.1 — local API server for family ops data",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js",
    "init-db": "node db/init.js",
    "seed": "node db/seed.js",
    "test": "NODE_ENV=test node --test --test-concurrency=1 tests/*.test.js",
    "smoke": "bash ../tools/smoke-test.sh"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "@clerk/backend": "^1.34.0",
    "@clerk/express": "^2.1.47",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "express": "^4.21.0"
  }
}
